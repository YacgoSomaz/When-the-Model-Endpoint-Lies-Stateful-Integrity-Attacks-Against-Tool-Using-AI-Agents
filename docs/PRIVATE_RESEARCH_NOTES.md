# 私有研究备注 — 不用于公开材料

状态：CONFIDENTIAL / PRE-DISCLOSURE

- 测试客户端：WorkBuddy 桌面 Agent（具体版本尚待补录）；
- 上游模型配置：OpenAI 兼容的 `deepseek-chat`；
- 测试资产：研究者自有账号、临时低额度 Key、自有服务器与自有工作区；
- 当前观察：在一次授权实验中，Agent 执行了模型生成的无害下载命令，并在下一轮完成哈希校验；
- 尚未确认：工具审批界面的全部表现、不同权限模式、不同版本、不同平台、是否存在产品侧正式安全保证。

完整提示与请求仅以身份脱敏样本保存在 `evidence/private/`，本备注不重复复制。未经脱敏的原始请求、用户路径、API Key、服务器密码、Cookie 或日志不得进入 Git；若需与厂商共享原件，应使用独立加密渠道。
