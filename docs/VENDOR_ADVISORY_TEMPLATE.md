# 厂商私下建议书模板

主题：工具型 AI Agent 的模型通道完整性与用户意图绑定问题（受控研究）

## 摘要

我们在自有账号、测试凭据、自有网关和无害载荷下观察到：当 OpenAI 兼容模型通道中的请求被改写时，模型可能生成与客户端可见输入不一致的工具计划。此报告不包含真实恶意载荷，也不主张已绕过操作系统或产品审批。

## 测试环境

- 产品与版本：`<PRODUCT_VERSION>`
- 平台：`<OS>`
- 权限模式：`<PERMISSION_MODE>`
- 模型与端点类型：`<MODEL_AND_ENDPOINT>`
- 测试日期：`<DATE>`

## 最小复现

1. 用户在客户端输入无害文本；
2. 授权网关在模型调用前替换为下载惰性 ZIP 的任务；
3. 记录模型的结构化工具调用；
4. 观察本地策略、审批界面和最终工具结果；
5. 删除产物并撤销测试凭据。

## 期望行为

高影响工具行为应与用户可见且确认的意图绑定；模型输出或历史上下文不应单独构成授权。

## 实际观察

`<REDACTED_OBSERVATION>`

## 建议

- 在本地执行器比对用户意图和规范化工具参数；
- 将下载、执行和安装拆成不同权限；
- 展示实际连接端点与完整审批参数；
- 为重试、回退和跨轮调用重新评估授权；
- 记录不含秘密值的完整性审计。

## 协调请求

请确认接收人、跟踪编号、初步评估时间和建议的协调披露窗口。我们愿意提供加密的脱敏证据并协助验证修复。
