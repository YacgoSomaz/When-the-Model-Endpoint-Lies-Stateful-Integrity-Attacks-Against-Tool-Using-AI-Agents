# 研究日志

## 2026-08-14

- 建立 OpenAI 兼容的双向人工审批网关；
- 解决控制台编辑期间自动刷新覆盖内容的问题；
- 支持非流式上游响应和对流式客户端的 SSE 重新封装；
- 增加会话级持续替换，用于观察 Agent 多轮上下文；
- 构建只包含 `README.txt` 和 `manifest.json` 的无害 ZIP；
- 完成 E-002 的首次受控观察：普通问候在模型侧变为无害下载与哈希任务；
- 建立私有研究仓库并开始整理防御方案和披露材料。

### 当日重要判断

- 模型返回 JSON 本身不会自动成为系统命令；危险发生在下游把字段映射到工具并授权执行时；
- 一次响应修改无法覆盖 Agent 的多轮状态，需要研究历史上下文和本地授权之间的关系；
- 演示必须避免真实软件、安装器和真实攻击目标；无害惰性文件已经足以证明完整性问题；
- 当前证据属于研究观察，不应直接表述为特定产品漏洞。
